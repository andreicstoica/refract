# Ensure native dependencies are installed
node-linker=hoisted
# Force platform-specific binaries for Vercel deployment
target_platform=linux
target_arch=x64
target_libc=gnu
# Ensure proper binary installation
prefer-offline=false
